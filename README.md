# PART I

`nano helloworld.cpp`

`git add helloworld`

`git commit -m ...`

`git push -u origin main`

# PART II

`git checkout -b ...`

`git merge patch1`

`git pull origin main`

`git log`

`git branch -d ...`

# PART III

`git merge`

`git push --force`

`clang-format -i --style=Mozilla`

